Test setup for initializing git through Stata project. The basic steps for doing this are:

- Stata > New project
- Create README.md file
- ! git init
- ! git commit -m "Initial commit :hatching_chick:"
